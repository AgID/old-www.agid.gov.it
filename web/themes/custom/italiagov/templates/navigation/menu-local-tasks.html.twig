{#
/**
 * @file
 * Default theme implementation to display primary and secondary local tasks.
 *
 * Available variables:
 * - primary: HTML list items representing primary tasks.
 * - secondary: HTML list items representing primary tasks.
 *
 * Each item in these variables (primary and secondary) can be individually
 * themed in menu-local-task.html.twig.
 *
 * @ingroup templates
 *
 * @see template_preprocess_menu_local_tasks()
 */
#}
{% if primary %}
  <h2 class="u-hiddenVisually">{{ 'Primary tabs'|t }}</h2>
  <ul class="drupalTabs--primary u-margin-r-top u-margin-r-bottom">{{ primary }}</ul>
{% endif %}
{% if secondary %}
  <h2 class="u-hiddenVisually">{{ 'Secondary tabs'|t }}</h2>
  <ul class="drupalTabs--secondary u-margin-r-top u-margin-r-bottom">{{ secondary }}</ul>
{% endif %}
